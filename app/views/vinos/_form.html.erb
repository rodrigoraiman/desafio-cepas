<%= form_with(model: vino, local: true) do |form| %>
  <% if vino.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vino.errors.count, "error") %> prohibited this vino from being saved:</h2>

      <ul>
      <% vino.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>
  <table>
    <div class="field">
      <%= form.label :cepas %>
      <%= form.collection_check_boxes(:cepa_ids, Cepa.all, :id, :name) do |b| %>
        <%= b.label class: 'label-checkbox' do %>
        <%= b.check_box + b.text %>
        <br>
        <%= form.text_field '', name:'vino[percentages][]' %>
        <% end %>
      <% end %>
    </div>
  </table>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
